/**
 * Preprocessing utilities stub for scaling, normalization, and tensor formatting.
 */

export function preprocessImageForModel(
  _canvas: HTMLCanvasElement,
  _targetWidth: number,
  _targetHeight: number
): Float32Array {
  return new Float32Array(0);
}
